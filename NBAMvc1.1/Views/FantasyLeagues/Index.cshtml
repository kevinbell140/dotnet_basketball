@model IEnumerable<NBAMvc1._1.Models.FantasyLeague>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CommissionerID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isFull)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CommissionerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isFull)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FantasyLeagueID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FantasyLeagueID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FantasyLeagueID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
