@model IEnumerable<NBAMvc1._1.Models.PlayerMyTeam>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlayerNav)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MyTeamNav)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerNav.PlayerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MyTeamNav.MyTeamID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  playerID = item.PlayerID, myTeamID = item.MyTeamID}) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
