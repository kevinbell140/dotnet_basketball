@model IEnumerable<NBAMvc1._1.Models.MyTeam>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserNav)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserNav.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MyTeamID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MyTeamID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MyTeamID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
